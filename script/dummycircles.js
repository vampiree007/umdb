new CircleProgress('.progress')
    .attr('max', 100)
    .attr('value', 20)
    .attr("textFormat", "percent")
    .attr("animation", "easeInOutCubic");

new CircleProgress('.progres')
    .attr('max', 100)
    .attr('value', 80)
    .attr("textFormat", "percent")
    .attr("animation", "easeInOutCubic");

new CircleProgress('.progres-3')
    .attr('max', 100)
    .attr('value', 75)
    .attr("textFormat", "percent")
    .attr("animation", "easeInOutCubic");

new CircleProgress('.progres-4')
    .attr('max', 100)
    .attr('value', 30)
    .attr("textFormat", "percent")
    .attr("animation", "easeInOutCubic");

new CircleProgress('.progres-5')
    .attr('max', 100)
    .attr('value', 60)
    .attr("textFormat", "percent")
    .attr("animation", "easeInOutCubic");